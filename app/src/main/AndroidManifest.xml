<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carersystem.carer">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".book_package"></activity>
        <activity android:name=".booking" />
        <activity android:name=".OTP" />
        <activity android:name=".Feedback" />
        <activity android:name=".Orderplan" />
        <activity android:name=".Carerview" />
        <activity
            android:name=".ConfirmationActivity"
            android:label="@string/title_activity_ConfirmationActivity"
            android:parentActivityName=".profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Purchase"
            android:label="@string/title_activity_Purchase"
            android:parentActivityName=".profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".homecare"
            android:label="@string/title_activity_homecare"
            android:parentActivityName=".profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".profile"
            android:label="@string/title_activity_profile" />
        <activity android:name=".register" />
        <activity android:name=".home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>